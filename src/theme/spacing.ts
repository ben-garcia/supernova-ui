const fontSize = 16;
const spacing = {
  xs: `${0.3 * fontSize}px`,
  sm: `${0.5 * fontSize}px`,
  md: `${1.25 * fontSize}px`,
  lg: `${2 * fontSize}px`,
  xl: `${3.25 * fontSize}px`,
  xxl: `${5.25 * fontSize}px`,
};

export default spacing;
